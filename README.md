# Agent Prelude

This repo contains the set of agent skills, rules, and sub-agents that I always load no matter what. This prelude serves to cut down on token use in various ways.

## Install (Cursor)

```bash
git clone git@github.com:Olian04/agent-prelude.git ~/.cursor/plugins/local/prelude
```
